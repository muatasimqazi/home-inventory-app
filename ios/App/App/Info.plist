<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>CAPACITOR_DEBUG</key>
	<string>$(CAPACITOR_DEBUG)</string>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
        <string>Schuaz</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
    <false/>
    <key>UISceneConfigurations</key>
    <dict>
      <key>UIWindowSceneSessionRoleApplication</key>
      <array>
        <dict>
            <key>UISceneConfigurationName</key>
            <string>Default Configuration</string>
            <key>UISceneDelegateClassName</key>
            <string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
            <key>UISceneStoryboardFile</key>
            <string>Main</string>
        </dict>
      </array>
    </dict>
	</dict>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>armv7</string>
	</array>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UIViewControllerBasedStatusBarAppearance</key>
	<true/>

	<!-- Google sign-in's own callback (src/components/native-auth-deep-
	     link-listener.tsx, src/app/sign-in/page.tsx's NATIVE_AUTH_CALLBACK_URL)
	     — routes com.schuaz.app://auth/callback back into this app once the
	     SFSafariViewController opened by Browser.open() finishes the OAuth
	     redirect chain. Android's equivalent is the custom_url_scheme
	     intent-filter in AndroidManifest.xml; unlike `cap add android`
	     (which auto-populates that from strings.xml), `cap add ios` does
	     NOT auto-generate this — without it, iOS has no handler registered
	     for the scheme at all, and the in-app Safari view fails with
	     "Safari cannot open the page because the address is not valid"
	     the moment the redirect hits it. -->
	<!-- @capacitor-community/fcm (via FirebaseMessaging) and Capacitor's own
	     core both do runtime method-swizzling of AppDelegate's
	     didRegisterForRemoteNotificationsWithDeviceToken to capture the
	     APNs token — with both enabled, PushNotifications.register()
	     resolved and neither the 'registration' nor 'registrationError'
	     event ever fired (confirmed on a real device: permission granted,
	     register() called, 60s+ of silence). Disabling Firebase's own
	     swizzling here and forwarding the token to it manually instead
	     (AppDelegate.swift) removes the conflict — Capacitor's swizzling,
	     now the only one touching this method, is untouched. -->
	<key>FirebaseAppDelegateProxyEnabled</key>
	<false/>

	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>com.schuaz.app</string>
			</array>
		</dict>
	</array>

	<!-- Capture flows (src/app/capture/*, src/app/scan, src/app/finance/scan)
	     use getUserMedia() directly in the WebView (no @capacitor/camera
	     plugin — see docs/Mobile App Addendum.md §2.2), same as Android.
	     Unlike Android's runtime permission denial, WKWebView silently
	     fails getUserMedia with no system prompt at all if this key is
	     missing, so it's required, not just good practice. -->
	<key>NSCameraUsageDescription</key>
	<string>Schuaz uses your camera to scan barcodes and photograph items, receipts, and documents for your household inventory.</string>

	<!-- Voice input (docs/Voice Input Addendum.md, voice-input-button.tsx)
	     uses getUserMedia()/MediaRecorder for audio, same reasoning. -->
	<key>NSMicrophoneUsageDescription</key>
	<string>Schuaz uses your microphone for voice dictation into notes and the AI assistant.</string>

	<!-- "Use my location" (household location, set-household-location-
	     sheet.tsx) uses navigator.geolocation directly, same reasoning. -->
	<key>NSLocationWhenInUseUsageDescription</key>
	<string>Schuaz uses your location to set your household's location for local weather alerts.</string>
</dict>
</plist>
